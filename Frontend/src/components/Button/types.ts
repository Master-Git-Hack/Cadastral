import { ButtonProps as BProps } from 'primereact/button';
import { ToggleButton as TProps } from 'primereact/togglebutton';
export interface ButtonProps extends BProps {

}
export interface ToggleButtonProps extends TProps { }